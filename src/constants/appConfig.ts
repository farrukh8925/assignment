export const LOCAL_VARIABLES: {
  tokenKey: string;
  tokenExpiry: string;
  maxPage: number;
  minPage: number;
} = {
  tokenKey: "supermetrics-token",
  tokenExpiry: "token-expiry",
  maxPage: 10,
  minPage: 1,
};
